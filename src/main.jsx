import React from "react";
import ReactDOM from "react-dom/client";
import ComingSoon from "./pages/CommingSoon"; // yahi component render hoga
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ComingSoon />
  </React.StrictMode>
);
